//let const

const constant = 2;

// constant = 1;

// hoisting *

// primitive, object
// primitive : 원시 자료형
// Number, string, boolean, undefined, null, Symbol

let a = 2;
let text = "3";

console.log(text + a);
console.log(text / a);

let c = 2 < 3;

console.log(c);

console.log(Boolean(undefined));
